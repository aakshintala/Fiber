[package]
name = "idle_tokio_mt"
version = "0.1.0"
edition = "2021"

[dependencies]
measure = { path = "../measure" }
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time", "macros", "net"] }
