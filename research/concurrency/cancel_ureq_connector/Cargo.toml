[package]
name = "cancel_ureq_connector"
version = "0.1.0"
edition = "2021"

[dependencies]
sse_server = { path = "../sse_server" }
ureq = { version = "3", default-features = false, features = ["rustls"] }
rcgen = "0.13"
rustls = { version = "0.23", default-features = false, features = ["std", "tls12", "ring"] }
