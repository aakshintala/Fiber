[package]
name = "http_minreq"
version = "0.1.0"
edition = "2021"

[dependencies]
sse_server = { path = "../sse_server" }
minreq = { version = "2", default-features = false, features = ["https-rustls"] }
