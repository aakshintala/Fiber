[package]
name = "hook-conversion-cost"
version = "0.0.0"
edition = "2021"

[dependencies]
mlua = { version = "0.12", features = ["lua54", "vendored", "serialize"] }
serde_json = "1"

[profile.release]
debug = false
