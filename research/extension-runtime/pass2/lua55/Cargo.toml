[workspace]
[package]
name = "stream-lua55"
version = "0.0.0"
edition = "2021"
[dependencies]
mlua = { version = "0.12", features = ["lua55", "vendored", "send", "serde"] }
serde_json = "1"
[profile.release]
debug = false
